# --- API Base URL (no trailing slash) ---
VITE_API_BASE=http://192.168.115.48:8085

# --- Auth/Header fields ---
VITE_CLIENT_ID=client-42
VITE_KEY_VERSION=v1

# HMAC secrets per key version (use one). In production do NOT expose secrets in the browser.
# Here is only for lab/dev against your gateway in a trusted environment.
VITE_SECRET_V1=replace-with-base64-secret-for-v1

# Canonical format toggles (match your server's verifier)
# METHOD\nPATH\nQUERY\nBODY_SHA256_HEX\nEPOCH_SECONDS\nNONCE\n
VITE_CANON_INCLUDE_EMPTY_QUERY=true
VITE_DATE_HEADER=epoch
